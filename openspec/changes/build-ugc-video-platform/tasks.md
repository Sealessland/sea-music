## 1. 工程与本地运行基线

- [x] 1.1 初始化 Go module、`cmd/api`、`cmd/worker` 和按领域分层的 `internal` 包结构，并添加禁止跨模块内部依赖的检查
- [x] 1.2 建立统一配置加载、敏感项校验、结构化日志、错误模型、请求 ID 和优雅关闭框架
- [x] 1.3 编排 PostgreSQL、Redis、S3 兼容存储、Kafka 兼容 broker 与观测组件，固定版本并配置健康检查
- [x] 1.4 建立数据库迁移、测试数据库清理和确定性 fixture 工具，提供单命令 bootstrap 与 verify
- [x] 1.5 定义 `/api/v1` OpenAPI 基线、分页/错误规范及契约校验任务

## 2. 身份、鉴权与流量保护

- [x] 2.1 以测试驱动实现用户表、唯一身份注册和自适应密码哈希，覆盖重复注册与敏感字段泄露
- [x] 2.2 实现登录、短期 access token、旋转 refresh session 和 session-family 重放撤销
- [x] 2.3 实现认证中间件、资源 ownership/role 授权策略并覆盖越权编辑测试
- [x] 2.4 基于 Redis 实现按路由类别、用户和地址的限流以及 `Retry-After`、降级和指标

## 3. 视频投稿最小纵切

- [x] 3.1 设计并迁移视频、源资产、rendition、处理任务及状态转换审计表
- [x] 3.2 以测试驱动实现版本化投稿状态机、非法转换保护和 optimistic concurrency
- [x] 3.3 实现 S3 上传授权、对象 key 隔离、finalize 元数据/校验和验证及幂等资产创建
- [x] 3.4 实现带租约的处理任务领取、续约、超时恢复和有界重试
- [x] 3.5 接入真实 `ffprobe`/`ffmpeg`，生成确定性 rendition 与封面并覆盖坏文件、超时和重启恢复测试
- [x] 3.6 实现审核结果接入、发布/撤回转换以及公开视频详情与短时播放 URL API
- [x] 3.7 添加“注册→上传→处理→发布→查询播放信息”的真实依赖端到端测试

## 4. 可靠事件基础设施

- [x] 4.1 迁移 Outbox/Inbox 表并定义带版本、event ID、trace context 和发生时间的事件 envelope
- [x] 4.2 实现与业务事务原子写入的 Outbox repository，并用回滚测试验证无孤儿事件
- [x] 4.3 实现带锁批量 dispatcher、broker ack 后确认、退避与 backlog 指标
- [x] 4.4 实现消费者幂等边界、分级重试、死信主题和受保护的查询/重放命令
- [x] 4.5 添加 dispatcher ack 窗口崩溃、重复消费、毒消息和 backlog 恢复集成测试
- [x] 4.6 将媒体处理和发布后副作用迁移到事件链路，并删除正式验收路径的同步旁路

## 5. 社交互动与计数一致性

- [x] 5.1 迁移关注、点赞、收藏唯一关系表并实现幂等创建/撤销及并发竞态测试
- [x] 5.2 实现 top-level 评论、受限回复、稳定游标及保留线程结构的删除 tombstone
- [x] 5.3 实现弹幕清洗、频控、时间窗索引和确定性分页 API
- [x] 5.4 消费互动事件更新公开计数缓存与持久化快照，并防止负数和重复增量
- [x] 5.5 实现权威关系对账修复任务、漂移指标及故意制造漂移后的恢复测试

## 6. 内容发现

- [x] 6.1 实现基于 `(published_at, id)` opaque cursor 的关注流及页间插入稳定性测试
- [x] 6.2 实现去重互动事件驱动的时间窗热门分数、衰减、快照及 Redis 故障降级
- [x] 6.3 实现基于关注、分类、新鲜度和热度的可解释推荐召回及冷启动多样性
- [x] 6.4 为所有 feed 建立发布状态、审核可见性和 block 关系的最终过滤层
- [x] 6.5 添加撤稿但缓存候选未过期、深游标和高关注基数下的发现链路集成测试

## 7. 可观测性、安全与运维

- [x] 7.1 为 HTTP、SQL、Redis、Outbox、broker 消费和 FFmpeg 任务接入 OpenTelemetry trace context
- [x] 7.2 实现有界标签的 RED/USE 指标、业务 backlog/计数漂移指标和本地仪表盘
- [x] 7.3 实现区分 liveness/readiness 的健康端点并覆盖必需与可选依赖故障
- [x] 7.4 加固上传输入、对象 key、令牌密钥、CORS、请求体限制、超时和日志脱敏
- [x] 7.5 编写 Outbox 堵塞、Redis 不可用、broker 重复投递和 Worker 中断的故障演练脚本与 runbook

## 8. 性能证据与项目交付

- [x] 8.1 生成有现实分布的用户、关注、视频和互动数据集并记录规模与生成 seed
- [x] 8.2 建立视频详情读、突发点赞和事件积压恢复三组可重复负载场景
- [x] 8.3 在固定环境采集基线延迟、吞吐、错误和饱和度，定位一个真实瓶颈并以回归测试驱动优化
- [x] 8.4 保存优化前后原始结果、环境配置和结论边界，验证指标与 trace 能解释瓶颈
- [x] 8.5 完善架构图、ADR、API 示例、演示脚本、故障恢复说明和面向简历的可证实成果摘要
- [x] 8.6 在全新环境执行 bootstrap、迁移、测试、端到端演示和负载 smoke，记录最终验证结果
